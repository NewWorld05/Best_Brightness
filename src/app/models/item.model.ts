export interface Item {
    itemName: string;
    quantity: number;
    itemCode: string;
    size: string;
    category: string;
    // Add any other properties as needed to match the structure of items in your Firebase collection
  }